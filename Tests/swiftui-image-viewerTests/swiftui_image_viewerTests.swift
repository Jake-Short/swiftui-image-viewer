import XCTest
@testable import swiftui_image_viewer

final class swiftui_image_viewerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftui_image_viewer().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
